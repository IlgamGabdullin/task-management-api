import { UserEntity } from './user.entity';

describe('UserEntity', () => {
  describe('validatepassword', () => {
    it('returns true as password valid', () => {
      expect('to').toEqual('to')
    })

    it('returns false as password invalid', () => {
      expect('to').toEqual('to')
    })
  })
});